// Generated by CoffeeScript 1.4.0
/**
  @fileoverview Wrapper for goog.json leveraging native implementation.
  goog.json will never switch to native, because it has different implementation
  in detail.
  todo
    add reviver and replacer args
*/

goog.provide('este.json');

goog.require('goog.json');

goog.scope(function() {
  var _ = este.json;

  /**
    @param {*} object The object to serialize.
    @return {string} A JSON string representation of the input.
  */
  _.stringify = function(object) {
    if (goog.global['JSON']) {
      return goog.global['JSON']['stringify'](object);
    } else {
      return goog.json.serialize(object);
    }
  };
  /**
    @param {string} str The JSON string to parse.
    @return {Object} The object generated from the JSON string.
  */

  _.parse = function(str) {
    if (goog.global['JSON']) {
      return goog.global['JSON']['parse'](str);
    } else {
      return goog.json.parse(str);
    }
  };
  /**
    todo: consider optimalization and low dash
    @param {*} a
    @param {*} b
  */

  _.equal = function(a, b) {
    return _.stringify(a) === _.stringify(b);
  };
});
