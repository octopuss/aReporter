// Coffe Class fixed for Closure Compiler by Este.js
// Generated by CoffeeScript 1.4.0
/**
  Inspired from Modernizr
  este.userAgent.propagateSupport s3j7lgxd4ruz1s3j7lgxd4ruz
  document.title = goog.dom.classes.has document.documentElement,
    s3j7lgxd4ruz2s3j7lgxd4ruz
*/

goog.provide('este.userAgent');

goog.require('goog.dom.classes');

goog.require('goog.string');

goog.scope(function() {
  var _ = este.userAgent;

  /**
    @type {Array.<string>}
  */
  _.vendorPrefixes = 'Webkit Moz O ms'.split(' ');
  /**
    @type {Element}
  */

  _.testElement = document.createElement('div');
  /**
    @type {Object}
  */

  _.testStyle = _.testElement.style;
  /**
    @param {string} prop
    @return {boolean}
  */

  _.supportCss = function(prop) {
    var prefix, vendorProp, _i, _len, _ref;
    if (prop in _.testStyle) {
      return true;
    }
    prop = prop.charAt(0).toUpperCase() + prop.substr(1);
    _ref = _.vendorPrefixes;
    for (_i = 0, _len = _ref.length; _i < _len; _i++) {
      prefix = _ref[_i];
      vendorProp = prefix + prop;
      if (vendorProp in _.testStyle) {
        return true;
      }
    }
    return false;
  };
  /**
    @param {string} prop
  */

  _.propagateSupport = function(prop) {
    if (!_.supportCss(prop)) {
      return;
    }
    prop = goog.string.toSelectorCase(prop);
    return goog.dom.classes.add(document.documentElement, 'support-' + prop);
  };
});
