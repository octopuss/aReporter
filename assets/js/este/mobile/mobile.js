// Generated by CoffeeScript 1.4.0

goog.provide('este.mobile');

goog.require('goog.userAgent');

goog.require('goog.userAgent.product.isVersion');

goog.require('goog.userAgent.platform');

goog.scope(function() {
  var _ = este.mobile;
  _.hideAddressBar = function() {
    return window.scrollTo(0, 1);
  };
  if (goog.userAgent.MOBILE) {
    _.tapEvent = 'touchstart';
  } else {
    _.tapEvent = 'mousedown';
  }
  /**
    https://gist.github.com/2829457
    Outputs a float representing the iOS version if user is using an iOS browser i.e. iPhone, iPad
    Possible values include:
      3 - v3.0
      4.0 - v4.0
      4.14 - v4.1.4
      false - Not iOS
  */

  _.iosVersion = parseFloat(('' + (/CPU.*OS ([0-9_]{1,5})|(CPU like).*AppleWebKit.*Mobile/i.exec(navigator.userAgent) || [0, ''])[1]).replace('undefined', '3_2').replace('_', '.').replace('_', '')) || false;
  /**
    Adds basic iPhone home icon link to the head
    @param {string} url
  */

  _.addiPhoneAppIcon = function(url) {
    var link;
    link = document.createElement('link');
    link.rel = 'apple-touch-icon';
    link.href = url;
    return document.head.appendChild(link);
  };
});
