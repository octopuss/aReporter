// Generated by CoffeeScript 1.4.0
/**
  @fileoverview este.App factory.
*/

goog.provide('este.app.create');

goog.require('este.App');

goog.require('este.app.Layout');

goog.require('este.router.create');

/**
  @param {string|Element} element
  @param {Array.<function(new:este.app.View)>} viewsClasses
  @param {boolean=} forceHash
  @return {este.App}
*/


este.app.create = function(element, viewsClasses, forceHash) {
  var layout, router, viewClass, views;
  element = goog.dom.getElement(element);
  views = (function() {
    var _i, _len, _results;
    _results = [];
    for (_i = 0, _len = viewsClasses.length; _i < _len; _i++) {
      viewClass = viewsClasses[_i];
      _results.push(new viewClass);
    }
    return _results;
  })();
  layout = new este.app.Layout(element);
  router = este.router.create(element, void 0, forceHash);
  return new este.App(views, layout, router);
};
