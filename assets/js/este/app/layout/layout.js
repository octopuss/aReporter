// Coffe Class fixed for Closure Compiler by Este.js
// Generated by CoffeeScript 1.4.0
/**
  @fileoverview Layout manager for este.app.View's. Views are rendered lazily,
  enter/exitDocument are called when view is shown/hidden.
*/



goog.provide('este.app.Layout');

goog.require('este.Base');

goog.require('goog.style');



  

  /**
    @param {Element} element
    @constructor
    @extends {este.Base}
  */


  este.app.Layout = function(element) {
    this.element = element;
    este.app.Layout.superClass_.constructor.apply(this, arguments);
  }

  goog.inherits(este.app.Layout, este.Base);

  /**
    @type {Element}
    @protected
  */


  este.app.Layout.prototype.element = null;

  /**
    @type {este.app.View}
    @protected
  */


  este.app.Layout.prototype.previousView = null;

  /**
    @param {este.app.View} view
    @param {Object=} params
  */


  este.app.Layout.prototype.show = function(view, params) {
    if (this.previousView) {
      this.previousView.exitDocument();
      goog.style.showElement(this.previousView.getElement(), false);
    }
    this.previousView = view;
    if (!view.getElement()) {
      view.render(this.element);
      return;
    }
    view.enterDocument();
    return goog.style.showElement(view.getElement(), true);
  };

  
